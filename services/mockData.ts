
/**
 * MOCK DATA REMOVED.
 * All application data is now fetched directly from the MongoDB database
 * via the server.js API endpoints.
 */
export const mockUsers = [];
export const mockPatients = [];
